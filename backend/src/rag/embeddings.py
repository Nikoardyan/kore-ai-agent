class Embeddings:
    def embed_text(self, text: str):
        # Placeholder for a future embedding model. The current RAG path uses the
        # lexical VectorStore so the app works offline without downloading models.
        return []
